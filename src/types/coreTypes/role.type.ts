export enum RoleType {
    UNKNOWN = -1,
    NOUSER = 0,
    SUPERADMIN = 1,    // Superadmin role
    INSTRUCTOR = 2,    // Instructor role
    LEARNER = 3,        // Learner role
    ORG_ADMIN = 4,         // Admin role
    ORG_HEAD_INSTRUCTOR = 5, // Head Instructor role
    GUEST = 6,       // Guest role
    SUPER_ADMIN
}